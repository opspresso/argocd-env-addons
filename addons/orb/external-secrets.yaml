apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: external-secrets-orb
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  project: addons
  destination:
    name: in-cluster
    namespace: addon-external-secrets
  source:
    repoURL: https://github.com/opspresso/argocd-env-addons.git
    targetRevision: HEAD
    path: charts/external-secrets
    helm:
      valueFiles:
        - values.yaml
        - orb/values-orb-demo.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
