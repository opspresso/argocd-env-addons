loki:
  fullnameOverride: loki

  deploymentMode: SingleBinary

  loki:
    auth_enabled: false

    commonConfig:
      replication_factor: 1

    storage:
      type: filesystem

    schemaConfig:
      configs:
        - from: "2024-04-01"
          store: tsdb
          object_store: filesystem
          schema: v13
          index:
            prefix: loki_index_
            period: 24h

    compactor:
      retention_enabled: true
      delete_request_store: filesystem

    limits_config:
      retention_period: 744h

  singleBinary:
    replicas: 1

    persistence:
      enabled: true
      size: 50Gi
      storageClass: gp3 # aws-ebs-sc

    resources:
      requests:
        cpu: 250m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1024Mi

  # Rules sidecar in the loki pod; unset it and the whole pod drops to
  # BestEffort no matter what singleBinary asks for. The limit has room to
  # spare - this is a python process, and it OOMKilled in four seconds at 64Mi.
  sidecar:
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        memory: 256Mi

  read:
    replicas: 0

  write:
    replicas: 0

  backend:
    replicas: 0

  gateway:
    enabled: false

  chunksCache:
    enabled: false

  resultsCache:
    enabled: false

  lokiCanary:
    enabled: false

  test:
    enabled: false
