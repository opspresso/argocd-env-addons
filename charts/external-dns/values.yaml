external-dns:
  fullnameOverride: external-dns

  sources:
    - ingress
    - service

  policy: sync # sync or upsert-only

  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      memory: 128Mi
